# Shared defaults for gradient-trained neural uplift models
seed: ${global_setings.seed}
hidden_dim: 200
n_layers: 3
dropout: 0.1
lr: 0.001
weight_decay: 0.00001
batch_size: 256
max_epochs: 50
patience: 5
val_fraction: 0.1
device: null
