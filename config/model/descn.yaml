descn:
  _target_: src.models.neural.descn.DESCN
  config:
    seed: ${global_setings.seed}
    hidden_dim: 200
    n_layers: 3
    dropout: 0.1
    lr: 0.001
    batch_size: 256
    max_epochs: 50
    patience: 5
    alpha: 0.5
