defaults:
  - model: [causalpfn, s_learner, t_learner, x_learner, dr_learner]
  - datasets: [criteo, hillstrom, lzd, orange, x5]
  - metrics: uplift
  - _self_

# ── Global experiment settings ─────────────────────────────────────────────
global_setings:
  seed: 42
