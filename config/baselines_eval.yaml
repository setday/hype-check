defaults:
  - model: [causalpfn, s_learner, t_learner, x_learner, dr_learner]
  - datasets: template           # → config/datasets/template.yaml
  - metrics: uplift            # → config/metrics/template.yaml
  - _self_

# ── Global experiment settings ─────────────────────────────────────────────
global_setings:
  seed: 42
